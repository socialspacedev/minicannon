# Copy to .env and fill in. .env is git-ignored.
#
# Discogs personal access token — used by `npm run discogs:sync` to fetch
# your collection. Generate one at https://www.discogs.com/settings/developers
# (Generate new token, no scopes needed for public collection access).
DISCOGS_TOKEN=
